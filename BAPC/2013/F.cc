#include <cstdio>

int main() {
  int t; scanf("%d", &t);
  while (t--) {
    int n, m; scanf("%d%d", &n, &m);
    for (int i = 0; i < m; ++i) {
      int a, b; scanf("%d%d", &a, &b);
    }
    printf("%d\n", n - 1);
  }

  return 0;
}
